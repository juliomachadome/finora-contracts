# dist is generated by `pnpm build` and versioned out of necessity (see CLAUDE.md).
# Marking it as `linguist-generated` collapses it in GitHub diffs and takes it out
# of the language statistics — a PR review should look at `src/`, not at the
# bundle.
dist/** linguist-generated=true -diff
pnpm-lock.yaml linguist-generated=true -diff
